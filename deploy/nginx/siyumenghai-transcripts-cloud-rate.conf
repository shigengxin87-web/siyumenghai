# Dedicated request budget for the formal cloud transcript job API. Media,
# images and the legacy transcript service must not consume this quota.
limit_req_zone $binary_remote_addr zone=transcripts_cloud_jobs:10m rate=60r/m;
