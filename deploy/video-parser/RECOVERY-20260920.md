# 2026-09-20 route recovery

The active nginx server had been restored from an older backup. The production frontend uses both `/api/video/profile` and `/api/transcripts-test-tencent/jobs`.

When restoring the server, include `deploy/video-parser/nginx-location.conf`, its rate zones and image location, and `services/tencent-transcript-test/nginx.conf` in their appropriate nginx contexts. The Tencent service listens on 127.0.0.1:2032. Without its location, POST falls through to static handling and returns 405.

Validate a new transcript submission and job polling from the production browser. Displaying a saved transcript does not validate this route. Preserve browser localStorage and the original hostname.
